<% include ../partials/header %>
<div class="container">

   <div class="row">

     <div class="col-md-3">
       <p class="lead">CampJS</p>
       <div class="list-group black">
         <li class="list-group-item active">Info1</li>
         <li class="list-group-item">Info2</li>
         <li class="list-group-item">Info3</li>
       </div>
     </div>
     <div class="col-md-9 black">
        <div class="thumbnail">
          <img class="img-responsive" src="../<%= campground.image %>" alt="">
          <div class="caption-full">
            <h4 class="pull-right">£10.00</h4>
            <h4><a class="black pull-left" href=""><%= campground.name %></a></h4>
            <br>
            <br>
            <p><%= campground.description %></p>
            <br>
            <p class="black pull-right">
                <em>Posted By <%= campground.author.username %></em>
            </p>
            <% if(currentUser && campground.author.id.equals(currentUser._id)) {  %>
              <a class="btn btn-warning btn-sm pull-left"href="/campgrounds/<%= campground._id %>/edit">Edit</a>
              <form id="delete-form" action="/campgrounds/<%= campground._id %>?_method=DELETE" method="POST">
                <button class="btn btn-danger btn-sm pull-left">Delete</button>
              </form>
            <% } %>
            <br>
          </div>
        </div>
        <div class="well">
              <div class="text-right">
                <a class="btn btn-success" href="/campgrounds/<%= campground._id %>/comments/new">Add new comments</a>
              </div>
              <hr>
              <% campground.comments.forEach(function(comment){ %>
                  <div class="row">
                    <div class="col-md-12">
                      <span class="pull-left"><strong><%= comment.author.username %></strong></span>
                      <span class="pull-right">10 days ago</span>
                      <br>
                      <br>
                      <p><%= comment.text %></p>
                    </div>
                  </div>
              <% }); %>


        </div>



     </div>

   </div>








</div>

<% include ../partials/footer %>
